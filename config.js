window.DASHBOARD_CONFIG = {
  owner: "beluuwuleo1-eng",
  repository: "open-dashboard-",
  pollIntervalMs: 12000,
  issueLimit: 30,
  // Questions and requests are represented by issues with these labels because
  // the public REST API cannot reliably read Discussions without authentication.
  questionLabels: ["question", "request", "help wanted"],
};
